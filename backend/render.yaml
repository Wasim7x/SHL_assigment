services:
  - type: web
    name: shl-assessment-recommender
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PORT
        value: "8000"
    healthCheckPath: /health
    plan: free
